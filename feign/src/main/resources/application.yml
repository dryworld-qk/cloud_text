server:
  port: 8050
spring:
  application:
    name: feign
eureka:
  client:
    service-url:
      defualZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
feign:
  hystrix:
    enabled: true # 是否启动熔断器